name: Develop Workflow

on:
  push:
    branches:
      - develop

jobs:
  lamassu_ca_unit_testing:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0