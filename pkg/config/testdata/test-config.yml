logs:
  level: "info"
event_bus:
  log_level: "debug"
  provider: amqp
  enabled: true
  amqp:
    protocol: amqp #amqp | amqps
    hostname: rabbitmq
    port: 5672
    basic_auth:
      enabled: true
      username: admin
      password: admin
dms_manager_client:
  log_level: debug
  auth_mode: noauth
  protocol: http
  hostname: dms-manager
  port: 8085
device_manager_client:
  log_level: debug
  auth_mode: noauth
  protocol: http
  hostname: device-manager
  port: 8085
ca_client:
  log_level: debug
  auth_mode: noauth
  protocol: http
  hostname: ca
  port: 8085
connector_id:  aws.580561117445
aws_config:
  access_key_id: "****"
  secret_access_key:  "****"
  region:  eu-west-1